const ShowTickets = ({ event }) => {
  console.log(event);

  return (
    <div className="text-center">
      <h1>View, Add, change and remove tickets from here</h1>
    </div>
  );
};

export default ShowTickets;
